<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.promoteprovider.addbrowser">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AddBrowser">
        <activity android:name=".upwork"></activity>
        <activity android:name=".freelancer" />
        <activity android:name=".fiverr" />
        <activity android:name=".bongoAcademy" />
        <activity android:name=".firebase" />
        <activity android:name=".snapchat" />
        <activity android:name=".linkedin" />
        <activity android:name=".instagram" />
        <activity android:name=".facebook" />
        <activity android:name=".youtube" />
        <activity android:name=".Setting" />
        <activity android:name=".Sites" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>